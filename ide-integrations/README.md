# IDE integration sandbox

This directory certifies the shared Zed Package Manager IDE contract without using a developer's real package home, credentials, or registry.

The workflow checks the two live integrations and the five publication candidates using their native toolchains. Full GUI smoke tests remain a promotion gate for dedicated repositories and clean editor instances.
